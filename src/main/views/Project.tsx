import { defineComponent } from "vue";

export default defineComponent({
    name: "workspace",

    setup(props: any, {}) {
        return () => {
            return <div></div>;
        };
    },
});
